//@ts-check
const i : number = 10

/**
 * Sum two numbers
 * @param {number} a 
 * @param {number} b 
 * @returns {number}
 */
const sum = (a : number, b : number) => {
  return a + b
}

console.log(i)